# CI Utilities

This directory contains tooling to support our CI setup and deploy Nubus into a
prepared environment.

It depends on the repository `nubus-helm` which has to be available in the
subpath `nubus-helm`.
