{{/*
SPDX-License-Identifier: AGPL-3.0-only
SPDX-FileCopyrightText: 2023 - 2025 Univention GmbH
*/}}

---
apiVersion: "v1"
kind: "Secret"
metadata:
  name: {{ include "nubus-common.secrets.name" (dict "context" .) | quote }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels:
    {{- include "common.labels.standard" ( dict "customLabels" .Values.additionalLabels "context" . ) | nindent 4 }}
  {{- if .Values.additionalAnnotations }}
  annotations:
    {{- include "common.tplvalues.render" ( dict "value" .Values.additionalAnnotations "context" . ) | nindent 4 }}
  {{- end }}
data:
  PROVISIONING_API_PASSWORD: {{ .Values.provisioningApi.auth.password | b64enc | quote }}
